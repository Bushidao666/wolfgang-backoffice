# Autentique Service (Nest.js) - exemplo de variáveis de ambiente
# Copie para `autentique-service/.env` se for rodar localmente sem Docker.

PORT=4002
NODE_ENV=development
SERVICE_NAME=autentique-service

REDIS_URL=redis://localhost:6380
CORS_ORIGIN=http://localhost:3000

# Supabase
SUPABASE_URL=http://localhost:54321
SUPABASE_SERVICE_ROLE_KEY=

# Autentique
# ⚠️ As credenciais (api_key/webhook_secret) NÃO são lidas de env.
# Configure pelo Backoffice em `core.integration_credential_sets` + `core.company_integration_bindings`.
# Se precisar customizar o host (proxy), use `config.base_url` no credential set.

# Observabilidade (opcional)
PROMETHEUS_ENABLED=true
